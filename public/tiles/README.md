# Mahjong Tile SVGs

These SVG tiles are downloaded from Wikimedia Commons (see the script in `script/fetch_tiles.sh`).

Suggested source category: Unicode Mahjong tiles (color).

Before distributing, verify the license on each file’s Commons page. The files referenced by the
fetch script are typically marked as public domain, but it’s best to confirm.
