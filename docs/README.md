# GitHub Pages

This folder contains the static site for GitHub Pages.

To publish:
1. Commit the `docs/` folder.
2. In GitHub repo settings, set Pages source to **main /docs**.

Open `docs/index.html` locally to preview.
